/*
let red= document.querySelectorAll(".cabecera"); // solo tomamos el primero elemento que tenga parametro de entrada
// si tiene mas de uno tenemos que poner el [] porque guardandolo en un array
red.forEach((textarea) => {
    textarea.placeholder="escribe algo aqui...";
});
window.addEventListener("DOMContentLoaded", () => {
  const red = document.querySelectorAll(".cabecera");
  red.forEach((textarea, index) => {
    textarea.placeholder = `Escribe algo aquí... (${index + 1})`;
  });
});
*/
